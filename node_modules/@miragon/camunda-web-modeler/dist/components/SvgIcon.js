import React from "react";
import { tss } from "tss-react";
var useStyles = tss.create(function () { return ({
    root: {
        height: "1.5rem",
        width: "1.5rem",
    },
}); });
var SvgIcon = function (props) {
    var _a = useStyles(), classes = _a.classes, cx = _a.cx;
    return (React.createElement("svg", { className: cx(classes.root, props.className) },
        React.createElement("path", { d: props.path })));
};
export default SvgIcon;
