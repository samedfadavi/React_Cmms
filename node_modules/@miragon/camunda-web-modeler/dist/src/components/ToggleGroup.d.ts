import React, { ReactNode } from "react";
export interface ToggleOption {
    id: string;
    node: ReactNode;
}
interface Props {
    options: ToggleOption[];
    onChange: (id: string) => Promise<void>;
    active: string;
    className?: string;
}
declare const ToggleGroup: React.FC<Props>;
export default ToggleGroup;
