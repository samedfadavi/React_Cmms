export var createBpmnIoEvent = function (event, data) { return ({
    source: "bpmnio",
    event: event,
    data: data,
}); };
export var isBpmnIoEvent = function (event) {
    return event.source === "bpmnio";
};
