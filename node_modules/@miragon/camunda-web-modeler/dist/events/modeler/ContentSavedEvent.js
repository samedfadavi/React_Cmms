var EventName = "content.saved";
export var createContentSavedEvent = function (xml, svg, reason) { return ({
    source: "modeler",
    event: EventName,
    data: {
        xml: xml,
        svg: svg,
        reason: reason,
    },
}); };
export var isContentSavedEvent = function (event) {
    return event.source === "modeler" && event.event === EventName;
};
