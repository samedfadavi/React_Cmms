var EventName = "dmn.views.changed";
export var createDmnViewsChangedEvent = function (views, activeView) { return ({
    source: "modeler",
    event: EventName,
    data: {
        views: views,
        activeView: activeView
    }
}); };
export var isDmnViewsChangedEvent = function (event) { return (event.source === "modeler" && event.event === EventName); };
