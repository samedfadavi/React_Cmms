var EventName = "properties.panel.resized";
export var createPropertiesPanelResizedEvent = function (width) { return ({
    source: "modeler",
    event: EventName,
    data: { width: width }
}); };
export var isPropertiesPanelResizedEvent = function (event) { return (event.source === "modeler" && event.event === EventName); };
