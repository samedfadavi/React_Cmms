var EventName = "notification";
export var createNotificationEvent = function (message, severity) { return ({
    source: "modeler",
    event: EventName,
    data: {
        message: message,
        severity: severity
    }
}); };
export var isNotificationEvent = function (event) { return (event.source === "modeler" && event.event === EventName); };
