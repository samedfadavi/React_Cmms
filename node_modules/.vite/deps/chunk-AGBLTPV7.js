import {
  c,
  e,
  o as o4,
  o2 as o5,
  s as s2
} from "./chunk-PTZQ7V6T.js";
import {
  a as a2,
  o as o3
} from "./chunk-2CZAGFZF.js";
import {
  o as o2
} from "./chunk-V4RHVJ2H.js";
import {
  a
} from "./chunk-CL2YKTVD.js";
import {
  n as n2
} from "./chunk-TNRF7MOE.js";
import {
  o
} from "./chunk-CYGO6TZX.js";
import {
  n
} from "./chunk-NREDFKHH.js";
import {
  s
} from "./chunk-DCOKV3EO.js";

// ../node_modules/@arcgis/core/views/3d/webgl-engine/core/shaderModules/Float4PassUniform.js
var e2 = class extends a2 {
  constructor(r, e3) {
    super(r, "vec4", a.Pass, (s3, o6, t) => s3.setUniform4fv(r, e3(o6, t)));
  }
};

// ../node_modules/@arcgis/core/views/3d/webgl-engine/core/shaderLibrary/util/CameraSpace.glsl.js
function f(r) {
  r.fragment.uniforms.add(new e2("projInfo", (r2, o6) => c2(o6.camera))), r.fragment.uniforms.add(new e("zScale", (r2, o6) => m(o6.camera))), r.fragment.code.add(o2`vec3 reconstructPosition(vec2 fragCoord, float depth) {
return vec3((fragCoord * projInfo.xy + projInfo.zw) * (zScale.x * depth + zScale.y), depth);
}`);
}
function c2(r) {
  const o6 = r.projectionMatrix;
  return 0 === o6[11] ? s(n3, 2 / (r.fullWidth * o6[0]), 2 / (r.fullHeight * o6[5]), (1 + o6[12]) / o6[0], (1 + o6[13]) / o6[5]) : s(n3, -2 / (r.fullWidth * o6[0]), -2 / (r.fullHeight * o6[5]), (1 - o6[8]) / o6[0], (1 - o6[9]) / o6[5]);
}
var n3 = n();
function m(o6) {
  return 0 === o6.projectionMatrix[11] ? o(l, 0, 1) : o(l, 1, 0);
}
var l = n2();

// ../node_modules/@arcgis/core/chunks/SSAO.glsl.js
var u = 16;
function f2() {
  const r = new o5(), f3 = r.fragment;
  return r.include(o4), r.include(f), f3.include(c), f3.uniforms.add(new o3("radius", (e3, r2) => d(r2.camera))).code.add(o2`vec3 sphere[16] = vec3[16](
vec3(0.186937, 0.0, 0.0),
vec3(0.700542, 0.0, 0.0),
vec3(-0.864858, -0.481795, -0.111713),
vec3(-0.624773, 0.102853, -0.730153),
vec3(-0.387172, 0.260319, 0.007229),
vec3(-0.222367, -0.642631, -0.707697),
vec3(-0.01336, -0.014956, 0.169662),
vec3(0.122575, 0.1544, -0.456944),
vec3(-0.177141, 0.85997, -0.42346),
vec3(-0.131631, 0.814545, 0.524355),
vec3(-0.779469, 0.007991, 0.624833),
vec3(0.308092, 0.209288,0.35969),
vec3(0.359331, -0.184533, -0.377458),
vec3(0.192633, -0.482999, -0.065284),
vec3(0.233538, 0.293706, -0.055139),
vec3(0.417709, -0.386701, 0.442449)
);
float fallOffFunction(float vv, float vn, float bias) {
float f = max(radius * radius - vv, 0.0);
return f * f * f * max(vn - bias, 0.0);
}`), f3.code.add(o2`float aoValueFromPositionsAndNormal(vec3 C, vec3 n_C, vec3 Q) {
vec3 v = Q - C;
float vv = dot(v, v);
float vn = dot(normalize(v), n_C);
return fallOffFunction(vv, vn, 0.1);
}`), f3.uniforms.add(new s2("normalMap", (e3) => e3.normalTexture), new s2("depthMap", (e3) => e3.depthTexture), new o3("projScale", (e3) => e3.projScale), new s2("rnm", (e3) => e3.noiseTexture), new e("rnmScale", (r2, t) => o(m2, t.camera.fullWidth / r2.noiseTexture.descriptor.width, t.camera.fullHeight / r2.noiseTexture.descriptor.height)), new o3("intensity", (e3) => e3.intensity), new e("screenSize", (r2, t) => o(m2, t.camera.fullWidth, t.camera.fullHeight))), r.outputs.add("fragOcclusion", "float"), f3.code.add(o2`
    void main(void) {
      float depth = depthFromTexture(depthMap, uv);

      // Early out if depth is out of range, such as in the sky
      if (depth >= 1.0 || depth <= 0.0) {
        fragOcclusion = 1.0;
        return;
      }

      // get the normal of current fragment
      vec4 norm4 = texture(normalMap, uv);
      if(norm4.a != 1.0) {
        fragOcclusion = 1.0;
        return;
      }
      vec3 norm = vec3(-1.0) + 2.0 * norm4.xyz;

      float currentPixelDepth = linearizeDepth(depth);
      vec3 currentPixelPos = reconstructPosition(gl_FragCoord.xy, currentPixelDepth);

      float sum = 0.0;
      vec3 tapPixelPos;

      vec3 fres = normalize(2.0 * texture(rnm, uv * rnmScale).xyz - 1.0);

      // note: the factor 2.0 should not be necessary, but makes ssao much nicer.
      // bug or deviation from CE somewhere else?
      float ps = projScale / (2.0 * currentPixelPos.z * zScale.x + zScale.y);

      for(int i = 0; i < ${o2.int(u)}; ++i) {
        vec2 unitOffset = reflect(sphere[i], fres).xy;
        vec2 offset = vec2(-unitOffset * radius * ps);

        // don't use current or very nearby samples
        if( abs(offset.x) < 2.0 || abs(offset.y) < 2.0){
          continue;
        }

        vec2 tc = vec2(gl_FragCoord.xy + offset);
        if (tc.x < 0.0 || tc.y < 0.0 || tc.x > screenSize.x || tc.y > screenSize.y) continue;
        vec2 tcTap = tc / screenSize;
        float occluderFragmentDepth = linearDepthFromTexture(depthMap, tcTap);

        tapPixelPos = reconstructPosition(tc, occluderFragmentDepth);

        sum += aoValueFromPositionsAndNormal(currentPixelPos, norm, tapPixelPos);
      }

      // output the result
      float A = max(1.0 - sum * intensity / float(${o2.int(u)}), 0.0);

      // Anti-tone map to reduce contrast and drag dark region farther: (x^0.2 + 1.2 * x^4) / 2.2
      A = (pow(A, 0.2) + 1.2 * A*A*A*A) / 2.2;

      fragOcclusion = A;
    }
  `), r;
}
function d(e3) {
  return Math.max(10, 20 * e3.computeScreenPixelSizeAtDist(Math.abs(4 * e3.relativeElevation)));
}
var m2 = n2();
var v = Object.freeze(Object.defineProperty({ __proto__: null, build: f2, getRadius: d }, Symbol.toStringTag, { value: "Module" }));

export {
  e2 as e,
  f2 as f,
  d,
  v
};
//# sourceMappingURL=chunk-AGBLTPV7.js.map
